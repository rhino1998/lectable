<?xml version="1.0" encoding="utf-8"?>
<!-- updatePeriodMillis="0" - updates are pushed manually (see PlaybackWidget's doc comment)
     rather than polled, since playback state already changes far more often than any sane
     poll interval and Glance's updateAll()/update() are cheap to call on-demand. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="70dp"
    android:targetCellWidth="3"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
